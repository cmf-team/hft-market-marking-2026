; Naive baseline (старая SimpleCrossingStrategy под IStrategy-адаптером).
; Полезен как точка отсчёта при сравнении с AS.

lob_path             = sample_data/lob_sample.csv
trades_path          = sample_data/trades_sample.csv
strategy             = naive

initial_cash         = 1000000.0
max_events           = 0
progress_interval    = 50000
print_progress       = true

transaction_cost_bps = 1.0
slippage_bps         = 0.0
queue_priority       = true

summary_csv          = reports/naive_summary.csv
timeseries_csv       = reports/naive_timeseries.csv
timeseries_step      = 200
